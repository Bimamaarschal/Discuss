<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Postingan Diskusi</title>
</head>
<body>

    <h1>Postingan Diskusi</h1>

    <ul>
        <% posts.forEach(post => { %>
          <li>
            <h3><a href="/posts/<%= post._id %>"><%= post.title %></a></h3>
            <p><%= post.content %></p>
            <p>Likes: <%= post.likes %></p>
          </li>
        <% }); %>
      </ul>
      
</body>
</html>

